#include <stdio.h>

int main()
{
    int n;

    printf("Enter a non-negative integer number to print the multiplication table form 1 to that number: ");
    scanf("%d", &n);
    printf("\n");

    for (int i = 1; i <= n; i++)
    {
        for (int j = 1; j <=)
    }
}